#include <iostream>
#include <algorithm>
using namespace std;

int calac(int a, b){
    sort(b.begin(), b.end());
    return 0;
}

int main(){
    int n[1000],a,b;
    cout << "Line: ";
    cin >> a;
    for(int i = 1;i<a;i++){
        cout << "a[" << i << "] = ";
        cin >> b;
    }
    return 0;
}
